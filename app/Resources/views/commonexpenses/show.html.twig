{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commonexpense</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commonExpense.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commonexpenses_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commonexpenses_edit', { 'id': commonExpense.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
