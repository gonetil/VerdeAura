{% extends 'base.html.twig' %}

{% block body %}
    <h1>Invesmentexpenditure</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invesmentExpenditure.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ invesmentExpenditure.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('invesmentexpenditure_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('invesmentexpenditure_edit', { 'id': invesmentExpenditure.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
